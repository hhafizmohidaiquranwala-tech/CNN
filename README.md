# WhatsApp Clone Setup

1. Import `whatsapp.sql` into your database `rsoa_rsoa378_6`.
2. Ensure `index.php` is the landing page.
3. The `images` folder has been created for profile pictures.

## Features
- Signup/Login
- User List with Real-time Status and Last Message
- Real-time Chat (AJAX Polling)
- Mobile Responsive
- Dark Mode UI
